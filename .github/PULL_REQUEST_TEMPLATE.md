## What this changes

<!-- One logical change. What does this PR do? -->

## Why

<!-- What problem does it solve? Link the issue if there is one: fixes #NNN -->

## How to verify

<!-- Steps a maintainer can follow to confirm it works -->

## Checklist

- [ ] No credentials, tokens, `.env` files or real site URLs committed
- [ ] `go build ./...` passes (backend changes)
- [ ] `npm run build` passes (frontend changes)
- [ ] Diff is focused on one change
